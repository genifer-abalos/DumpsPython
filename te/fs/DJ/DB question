SELECT STUDENTS.STUDENT_NAME, MAJORS.MAJOR_NAME
FROM STUDENTS
    JOIN REGISTER
        ON REGISTER.STUDENT_ID = STUDENTS.STUDENT_ID
    JOIN MAJORS
        ON REGISTER.MAJOR_ID = MAJORS.MAJOR_ID
LIMIT 20